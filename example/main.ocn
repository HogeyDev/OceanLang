import "include/io.wv";
import "include/stdlib.wv";

int fib(int n) {
	if (n < 3) {
		return 1;
	}
	return fib(n - 1) + fib(n - 2);
}

int main() {
	int fibResult = fib(3);
	exit(fibResult);
	return 0;
}
